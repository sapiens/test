import { ShowStackPipe } from './show-stack.pipe';

describe('ShowStackPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowStackPipe();
    expect(pipe).toBeTruthy();
  });
});
